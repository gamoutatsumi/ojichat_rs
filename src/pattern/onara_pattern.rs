struct OnaraPattern {
    pattern: counted_array!([OjisanEmotion; _])
}

impl OnaraPattern {
    fn get_message(target: &str, emoji_num: &i64) -> String {
        let template = "";
        for emotion in self.pattern {
            template +=
        }
    }
}
